{
  "name": "@pact/contracts",
  "private": true,
  "description": "PACT Merkle root publication (spec §9). Foundry — no npm build.",
  "scripts": {
    "test": "forge test",
    "script:sepolia": "forge script script/Deploy.s.sol --rpc-url base_sepolia -vvvv"
  }
}
