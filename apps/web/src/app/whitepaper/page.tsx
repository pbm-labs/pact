import { WhitepaperView } from '@/components/whitepaper-view';

export const metadata = {
  title: 'Whitepaper — We build real',
  description:
    'PACT — Provenance of Accumulated Checkable Traces. Leftover streams on one tree. Anyone can recheck. It is not a score.',
};

export default function WhitepaperPage() {
  return <WhitepaperView />;
}
