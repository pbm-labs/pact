import { WhitepaperView } from '@/components/whitepaper-view';

export const metadata = {
  title: 'Whitepaper — We build real',
  description:
    'PACT is a public record of traces independent systems already emit. Mail reports and certificate logs, never blended into a score.',
};

export default function WhitepaperPage() {
  return <WhitepaperView />;
}
