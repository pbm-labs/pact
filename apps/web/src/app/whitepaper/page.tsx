import { WhitepaperView } from '@/components/whitepaper-view';

export const metadata = {
  title: 'Whitepaper — We build real',
  description:
    'PACT — Provenance of Accumulated Checkable Traces. A public record of leftover mail reports and certificate logs. Anyone can recheck. It is not a score.',
};

export default function WhitepaperPage() {
  return <WhitepaperView />;
}
