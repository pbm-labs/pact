import { WhyPactView } from '@/components/why-pact-view';

export const metadata = {
  title: 'Evidence, not authority — We build real',
  description:
    'Why the public record publishes leftover traces — mail reports and certificate logs — instead of asking you to trust a claim. That history cannot be manufactured after the fact.',
};

export default function DocsWhyPage() {
  return <WhyPactView />;
}
