import { DocsView } from '@/components/docs-view';

export const metadata = {
  title: 'Docs — We build real',
  description: 'PACT Protocol documentation.',
};

export default function DocsPage() {
  return <DocsView />;
}
