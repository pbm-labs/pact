import { DocsView } from '@/components/docs-view';

export const metadata = {
  title: 'Docs — We build real',
  description:
    'How the public record works — leftover traces from mail reports and certificate logs, how a domain gets one, how anyone checks, and what it does not claim.',
};

export default function DocsPage() {
  return <DocsView />;
}
