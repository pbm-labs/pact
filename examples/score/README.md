# Example: scoring a PACT public record

Informative application policy. **Not protocol.**

`example-score-0.1` and `example-display-0.1` consume only published record fields. They are not exported from `@pact/core`.

Write-up: [`docs/examples/scoring.md`](../../docs/examples/scoring.md).
