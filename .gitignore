# Dependencies & build
node_modules/
dist/
.next/
.open-next/
.wrangler/
*.tsbuildinfo
coverage/

# Environment (never commit secrets)
.env
.env.local
.env.*.local
.dev.vars
apps/web/.env.local
apps/web/.dev.vars

# OS & tooling
.DS_Store
.vercel

# Editor / local
.cursor/

# Secrets / keys (never commit)
*.pem
*.key

# Media
# Prefer R2/CDN for new large assets. The existing manifesto MP4 under
# apps/web/public/ is tracked in git (~11MB); do not blanket-ignore *.mp4.
